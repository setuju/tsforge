---
name: 🐛 Bug Report
about: Something is not working as expected
title: '[BUG] '
labels: bug
assignees: setuju
---

## 🐛 Bug Description

A clear and concise description of what the bug is.

## 🔁 Steps to Reproduce

1. Run `...`
2. Observe `...`

## ✅ Expected Behavior

What you expected to happen.

## ❌ Actual Behavior

What actually happened. Include the full output.

## 🖥️ Environment

```
OS         : (e.g. Ubuntu 24.04, macOS 15.1, WSL2)
Shell      : (e.g. bash 5.2, zsh 5.9)
Node.js    : (output of `node -v`)
TypeScript : (output of `npx tsc -v`)
tsgo       : (output of `npx tsgo -v` if installed, otherwise "not installed")
tsforge    : (output of `tsforge-help | head -1` or the commit hash)
```

## 📊 `tsc-where` Output

```
paste the output of `tsc-where` here
```

## 📎 Additional Context

Add any other context, screenshots, or log files here.
